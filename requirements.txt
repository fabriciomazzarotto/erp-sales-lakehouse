# Local development / testing
#
# IMPORTANTE (Windows): use Python 3.11, NÃO 3.12+, para rodar PySpark localmente.
# PySpark no Windows quebra com Python 3.12+ (worker Python morre sem traceback
# ao processar qualquer DataFrame) — bug conhecido, ver SPARK-53759. Databricks
# também tipicamente roda Python 3.10/3.11 nos runtimes atuais, então usar 3.11
# aqui também aproxima o ambiente local do cluster real.
# Criar o venv com: py -3.11 -m venv .venv
#
# pyspark/delta-spark são pareados de propósito (Delta 3.2.x <-> Spark 3.5.x).
# Ao migrar para Databricks, usar a versão de runtime do cluster (não este pin).
pyspark==3.5.3
delta-spark==3.2.1
python-dotenv==1.2.2
pyodbc==5.3.0
sqlalchemy==2.0.30

# AWS
boto3==1.34.144
awswrangler==3.9.0

# Data quality / testing
pytest==8.2.2
great-expectations==0.18.19

# Utilities
pandas==2.2.2
